name: UV Coverage Report

on:
  push:
    branches: [test_ci]

jobs:
  coverage:
#    runs-on: self-hosted
    runs-on: ubuntu-latest
    steps:
      # 1. Checkout code
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Set up Python
        run: |
          uv sync
          uv sync --group tests

      - name: Run tests
        run: uv run pytest tests

      - name: Run tests via Makefile
        run: make cov || true

      - name: Extract total coverage
        id: cov
        run: |
          COVERAGE=$(grep -Eo "TOTAL\s+[0-9]+\s+[0-9]+\s+[0-9]+%" coverage.txt | awk '{print $4}')
          echo "coverage=$COVERAGE" >> $GITHUB_OUTPUT

      - name: Comment coverage details
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          header: uv-coverage-report
          message: |
            🧪 **UV Coverage Report**
            **Total coverage:** ${{ steps.cov.outputs.coverage }}

            ---
            ### 🔍 Files missing coverage (changed files only):
            ```markdown
            $(cat diff-report.md)
            ```

            📄 [View full HTML report](diff-cover.html)

            ✅ Generated automatically via UV + Make cov
